program Test {
  void Main() {
    	int x, y, z;
    	x := 10;
    	y := 5;

    	z := x > y ? 2 : 1;
    	write x;
    	write y;
    	write z;

    int i, j;
    	for (i := 1, j:= 1; i <= 5; i := i + 1, j := i) {
      write i, j;
    	}
  }
}